print("Adelaide".strip("A"))  # Strip only remove character at the beginning or end of line
print("Adelaide".strip("del"))  # will remove the "de" and ignore the l
print("Adelaiden\n".strip("\n"))  # removes the \n only